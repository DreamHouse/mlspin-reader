<div class="signin_page">
  <div class="jumbotron">
    <div class="containter">
      <div class="row">
        <div class="col-md-10">
          <div class="signin_form">
            <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
              <% unless resource.errors.empty? %>
              <div class="error_container">
                <h3><%= t('errors.messages.not_saved.one')%></h3>
                <ul>
                  <% resource.errors.messages.each do |error_element, messages| %>
                    <li><%= messages.join("，") %></li>
                  <% end %>
                </ul>
              </div>
              <% end %>
              <div class="containter">
                <div class="row signin_form_row">
                  <div class="col-md-2"><%= f.label t('devise.email') %></div>
                  <div class="col-md-10"><%= f.email_field :email, autofocus: true %></div>
                </div>
            
                <div class="row signin_form_row">
                  <div class="col-md-2"><%= f.label t('devise.password') %></div>
                  <div class="col-md-10"><%= f.password_field :password, autocomplete: "off" %></div>
                </div>
            
                <div class="row signin_form_row">
                  <div class="col-md-2"><%= f.label t('devise.password_confirmation') %></div>
                  <div class="col-md-10"><%= f.password_field :password_confirmation, autocomplete: "off" %></div>
                </div>

                <div class="row signin_form_row">
                  <div class="col-md-12"><%= f.submit t('devise.sign_up'), :class => "btn btn-primary" %></div>
                </div>
              </div>
            <% end %>
          </div>
        <div class="col-md-2">
          <!-- add some graph here -->
        </div>
        </div>
    <!-- <%= render "devise/shared/links" %> -->
  </div>
</div>
</div>